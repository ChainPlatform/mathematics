name: Daily XSMN Scrape

on:
  schedule:
    - cron: '15 11 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - run: npm install axios cheerio

      - name: Scrape XSMN
        run: |
          node -e "
            const fs = require('fs'), path = require('path');
            const { scrapeToday } = require('./scripts/scrapeMNToday');

            scrapeToday()
              .then(data => {
                const file = './results/xsmn.json';
                const prev = fs.existsSync(file) ? JSON.parse(fs.readFileSync(file)) : [];

                const map = new Map();

                prev.forEach(i => map.set(i.date + '_' + i.province_id, i));
                data.forEach(i => map.set(i.date + '_' + i.province_id, i));

                const finalData = Array.from(map.values()).sort((a,b)=>{
                  return new Date(b.date.split('-').reverse().join('-')) -
                         new Date(a.date.split('-').reverse().join('-'));
                });

                fs.writeFileSync(file, JSON.stringify(finalData,null,2));
                console.log('✅ Updated XSMN');
              })
              .catch(e => { console.error(e); process.exit(1); });
          "

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/xsmn.json
          git diff --quiet --cached || git commit -m "📊 Update XSMN @ $(date -u +'%Y-%m-%d')" 
          git push